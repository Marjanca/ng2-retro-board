export class NoteCoords {

    public top: number;
    public left: number;
    public zIndex: number;

    constructor(top: number, left: number, zIndex: number) {
        this.top = top;
        this.left = left;
        this.zIndex = zIndex;
    }
}
